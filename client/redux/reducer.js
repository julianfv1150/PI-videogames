

const userReducer = (state = {
    isLogin: false    
}, action) => {

    switch(action.type){
        default:
            return state
    }
}

const vgamesReducer = (state = {
    isLogin: false    
}, action) => {

    switch(action.type){
        default:
            return state
    }
}

export {
    userReducer,
    vgamesReducer
};